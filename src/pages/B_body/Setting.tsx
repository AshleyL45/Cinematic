import {FC} from 'react';
import Pages from "../../components/layout/Pages";

const Setting: FC<{}> = ({}) => {
    return (
        <Pages title={"Setting"}>
            <h1>Coucou, je suis dans Setting</h1>
        </Pages>
    );
};

export default Setting;
